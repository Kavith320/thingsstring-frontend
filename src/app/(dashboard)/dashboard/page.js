"use client";

export default function DashboardPage() {
  return (
    <div>
      <div className="text-2xl font-semibold">Dashboard</div>
      <p className="mt-2 text-gray-600">
        Next step: show device counts + online/offline + latest alerts.
      </p>
    </div>
  );
}
